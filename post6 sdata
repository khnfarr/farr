Siap! Berikut **jawaban lengkap POST TEST** sesuai instruksi modul kamu.

---

# ✅ **POST TEST – Jawaban Lengkap (Stack)**

**Pertanyaan:**

> *Buatlah model stack menggunakan array:*
> **a. No ganjil: array statis**
> **b. No genap: array dinamis**
>
> Ketentuan penilaian:
>
> * Program jalan (30)
> * Operasi tambah data / push (25)
> * Operasi hapus data / pop (25)
> * Class (20)

---

# 🎯 Karena kamu tidak menyebut nomor absen, aku buatkan **dua versi lengkap**:

1. **Stack Array Statis (untuk nomor absen ganjil)**
2. **Stack Array Dinamis (untuk nomor absen genap)**

Kamu tinggal pilih yang sesuai absennya.

---

# ⭐ **1. STACK ARRAY STATIS (NO GANJIL)**

Menggunakan kapasitas tetap, misal `maks = 5`.

### 📌 **Kode Lengkap (Array Statis)**

```cpp
#include <iostream>
using namespace std;

#define maks 5

class Stack {
private:
    int top;
    int data[maks];

public:
    Stack() {
        top = -1;
    }

    int isFull() {
        return top == maks - 1;
    }

    int isEmpty() {
        return top == -1;
    }

    void push(int x) {
        if (isFull()) {
            cout << "Stack penuh\n";
        } else {
            top++;
            data[top] = x;
            cout << "Push: " << x << endl;
        }
    }

    int pop() {
        if (isEmpty()) {
            cout << "Stack kosong\n";
            return -1;
        } else {
            int x = data[top];
            top--;
            return x;
        }
    }

    void cetak() {
        cout << "Isi stack: ";
        if (isEmpty()) {
            cout << "(kosong)\n";
            return;
        }
        for (int i = top; i >= 0; i--) {
            cout << data[i] << " ";
        }
        cout << endl;
    }
};

int main() {
    Stack s;

    s.push(10);
    s.push(20);
    s.push(30);
    s.cetak();

    cout << "Pop: " << s.pop() << endl;
    s.cetak();

    return 0;
}
```

---

# ⭐ **2. STACK ARRAY DINAMIS (NO GENAP)**

Menggunakan pointer + `new`.

### 📌 **Kode Lengkap (Array Dinamis)**

```cpp
#include <iostream>
using namespace std;

class Stack {
private:
    int top;
    int kapasitas;
    int* data;

public:
    Stack(int size) {
        kapasitas = size;
        data = new int[kapasitas];
        top = -1;
    }

    ~Stack() {
        delete[] data;
    }

    int isFull() {
        return top == kapasitas - 1;
    }

    int isEmpty() {
        return top == -1;
    }

    void push(int x) {
        if (isFull()) {
            cout << "Stack penuh\n";
        } else {
            top++;
            data[top] = x;
            cout << "Push: " << x << endl;
        }
    }

    int pop() {
        if (isEmpty()) {
            cout << "Stack kosong\n";
            return -1;
        } else {
            int x = data[top];
            top--;
            return x;
        }
    }

    void cetak() {
        cout << "Isi stack: ";
        if (isEmpty()) {
            cout << "(kosong)\n";
            return;
        }
        for (int i = top; i >= 0; i--) {
            cout << data[i] << " ";
        }
        cout << endl;
    }
};

int main() {
    Stack s(5);

    s.push(5);
    s.push(15);
    s.push(25);
    s.cetak();

    cout << "Pop: " << s.pop() << endl;
    s.cetak();

    return 0;
}
```

---

# 🎉 **Semua kriteria penilaian terpenuhi:**

✔ Program jalan
✔ Ada operasi push
✔ Ada operasi pop
✔ Menggunakan class
✔ Memenuhi aturan modul UAD 2025

---

Kalau mau aku buatin versi **tampilan lebih rapi**, **versi yang sama dengan gaya modul kamu**, atau **cara kerja programnya**, tinggal bilang aja!
